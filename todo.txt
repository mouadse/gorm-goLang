# UM6P_FIT Backend Development Todo List
# Aligned with roadmap_plan.md - Focus on backend business logic only

================================================================================
PHASE 0: ARCHITECTURE HARDENING
================================================================================

[x] 1. Service Layer
    - [x] Create services/ or internal/services/ directory
    - [x] Move analytics, nutrition target logic, integration rules, exports, and auth session logic out of handlers
    - [x] Keep handlers thin: parse request, authorize, call service, serialize response
    - [x] Make business logic unit-testable
    - [x] Clear separation between repository/query code and business rules
    Files:
      - services/workout_analytics.go
      - services/nutrition_targets.go
      - services/integration_rules.go
      - services/auth_sessions.go
      - services/exports.go
      - services/services_test.go

================================================================================
PHASE 1: AUTHENTICATION COMPLETION
================================================================================

[x] 2. Refresh Tokens & Session Management
    Schema:
      - refresh_tokens table (id, user_id, hashed_token, user_agent, ip_address, expires_at, revoked_at, created_at)
      - user_sessions table (id, user_id, session_id, user_agent, last_ip, created_at, expires_at)
    Endpoints:
      - POST /v1/auth/refresh
      - POST /v1/auth/logout
      - GET /v1/auth/sessions
      - DELETE /v1/auth/sessions/{id}
    Business Rules:
      - Rotate refresh tokens on every refresh
      - Revoke token chain on suspicious reuse
      - Allow logout of current session and all sessions
      - Keep access tokens stateless and short-lived
    Tests:
      - Refresh success
      - Refresh with revoked token
      - Refresh after expiry
      - Logout invalidates token
      - Multiple sessions per user

================================================================================
PHASE 2: WORKOUT ANALYTICS FOUNDATION
================================================================================

[x] 3. Workout Analytics Services
    Business Logic:
      - [x] Previous workout comparison for same exercise
      - [x] Personal record detection (heaviest set, best estimated 1RM, best volume set, best rep performance)
      - [x] Estimated 1RM using Epley formula
      - [x] Completed-set volume calculation (reps * weight)
      - [x] Per exercise session volume (sum of completed set volumes)
      - [x] Per workout volume (sum of exercise session volumes)
      - [x] Per week volume (sum of workout volumes)
      - [x] Weekly workout frequency
      - [x] Workout-type frequency
    Files:
      - services/workout_analytics.go
      - DTOs for derived workout data

[x] 4. Workout Read-Model Endpoints
    Endpoints:
      - [x] GET /v1/users/{user_id}/records
      - [x] GET /v1/users/{user_id}/workout-stats
      - [x] GET /v1/exercises/{id}/history
      - [x] GET /v1/users/{user_id}/activity-calendar
    Business Rules:
      - [x] Records include heaviest set, best estimated 1RM, best volume set, best rep performance
      - [x] Exercise history includes ordered past sessions with related sets
      - [x] Activity calendar marks workout, meal, and weight-entry days
    Tests:
      - [x] Fixed fixture data with exact expected PR outputs
      - [x] Date-range filtering
      - [x] Authorization boundaries

[x] 5. Streaks & Adherence Logic
    Business Logic (derived from existing date data, no new tables):
      - [x] Workout streak = consecutive weeks with at least one workout
      - [x] Meal streak = consecutive days with at least one meal
      - [x] Weigh-in streak = consecutive days or weeks with a weight entry
      - [x] Adherence summaries for last 7, 30, and 90 days
    Endpoints:
      - [x] GET /v1/users/{user_id}/streaks
      - [x] Or include streaks in dashboard/stat endpoints
    Tests:
      - [x] Gap-day and gap-week behavior
      - [x] Timezone-safe date handling

================================================================================
PHASE 3: WORKOUT DOMAIN EXPANSION
================================================================================

[✓] 6. Cardio Entries (First-Class Model) - COMPLETE ✓
    Schema:
      - workout_cardio_entries table ✓
      Fields: id, workout_id, modality, duration_minutes, distance, distance_unit,
              pace, calories_burned, avg_heart_rate (optional), notes, created_at, updated_at
    Endpoints:
      - GET /v1/workouts/{id}/cardio ✓
      - POST /v1/workouts/{id}/cardio ✓
      - PATCH /v1/workout-cardio/{id} ✓
      - DELETE /v1/workout-cardio/{id} ✓
    Business Rules:
      - Support running, cycling, walking, swimming, rowing, generic cardio ✓
      - Allow workout to contain both resistance exercises AND cardio entries ✓
    Tests:
      - Mixed resistance + cardio workout ✓
      - Invalid negative values ✓
      - Derived calorie adjustment compatibility later ✓
      - Authorization checks ✓
      - Cascade delete ✓
    Files Created:
      - models/workout_cardio_entry.go ✓
      - api/cardio_handlers.go ✓
      - api/cardio_handlers_test.go ✓
      - models/workout_cardio_entry_test.go ✓
      - database/migrations.go (updated) ✓

[✓] 7. Workout Templates - COMPLETE ✓
    Schema:
      - workout_templates table ✓
      - workout_template_exercises table ✓
      - workout_template_sets table ✓
      Fields: defined in models/workout_template.go ✓
    Endpoints:
      - POST /v1/workout-templates ✓
      - GET /v1/workout-templates ✓
      - GET /v1/workout-templates/{id} ✓
      - PATCH /v1/workout-templates/{id} ✓
      - DELETE /v1/workout-templates/{id} ✓
      - POST /v1/workout-templates/{id}/apply ✓
    Business Rules:
      - Applying a template creates a normal workout snapshot ✓
      - Editing a template never mutates existing workouts ✓
      - Exercise ordering is preserved ✓
    Tests:
      - Template apply creates correct workout hierarchy ✓
      - Ordering is preserved ✓
      - Auth scoping ✓
      - Weight values preserved ✓
      - Notes preserved ✓
      - Template edits don't affect created workouts ✓
    Files Created:
      - models/workout_template.go ✓
      - models/workout_template_test.go ✓
      - services/workout_template.go ✓
      - services/workout_template_test.go ✓
      - api/template_handlers.go ✓
      - database/migrations.go (updated) ✓
      - api/server.go (routes added) ✓

[ ] 8. Workout Programs (Admin)

[ ] 7. Workout Templates
    Schema:
      - workout_templates table (id, owner_id, name, type, notes, created_at, updated_at)
      - workout_template_exercises table (id, template_id, exercise_id, order, sets, reps, weight, rest_time)
      - workout_template_sets table (id, template_exercise_id, set_number, reps, weight, rest_seconds)
    Endpoints:
      - POST /v1/workout-templates
      - GET /v1/workout-templates
      - GET /v1/workout-templates/{id}
      - PATCH /v1/workout-templates/{id}
      - DELETE /v1/workout-templates/{id}
      - POST /v1/workout-templates/{id}/apply
    Business Rules:
      - Applying a template creates a normal workout snapshot
      - Editing a template never mutates existing workouts
    Tests:
      - Template apply creates correct workout hierarchy
      - Ordering is preserved
      - Auth scoping

[✓] 8. Workout Programs (Admin) - MODELS COMPLETE ✓
    Schema:
      - workout_programs table ✓
      - program_weeks table ✓
      - program_sessions table ✓
      - program_assignments table ✓
      All fields defined in models/workout_program.go ✓
    Model Validation:
      - UUID generation ✓
      - Required field validation ✓
      - Default status handling ✓
      - All model tests passing ✓
    Files Created:
      - models/workout_program.go ✓
      - models/workout_program_test.go ✓
      - database/migrations.go (updated) ✓
    
    Note: Admin CRUD endpoints, assignment logic, and user-facing endpoints
    are ready for implementation. The schema and models are fully tested
    and in place as a foundation for future admin program management.

================================================================================
PHASE 4: NUTRITION DATA EXPANSION
================================================================================

[✓] 9. Food Catalog Expansion - COMPLETE ✓
    Implementation:
      - Add import tooling for USDA data ✓
      - Make ingestion idempotent ✓
      - Add normalization and deduplication rules ✓
    Requirements:
      - 500+ verified food items ✓ (USDA Foundation Foods dataset)
      - Better search quality ✓ (category/source filters added)
      - Stable serving-unit handling ✓ (portions from USDA, default 100g fallback)
    Deliverables:
      - Import scripts ✓
      - Seed/import docs ✓
      - Validation checks for duplicate foods ✓ (FdcID unique index)
    Files Created:
      - scripts/import_usda.go ✓
      - services/usda_import.go ✓
      - services/usda_import_test.go ✓
      - services/seed_nutrients.go ✓
      - api/food_import_handlers.go ✓
      - models/food.go (updated: FdcID, Category, Source fields) ✓
      - api/food_handlers.go (updated: category/source filters) ✓
      - api/server.go (updated: import route + service) ✓

[✓] 10. Normalized Micronutrient Support - COMPLETE ✓
    Schema:
      - nutrients table (id, code, name, unit, category) ✓
      - food_nutrients table (id, food_id, nutrient_id, amount_per_100g) ✓
    Business Rules:
      - Level 1 tracks 19 key nutrients through this schema ✓
      - Level 2 can expand to 84 nutrients without redesign ✓
    Nutrients tracked (Level 1):
      - Vitamins: A, C, D, E, K, B1, B2, B3, B5, B6, B12, Folate ✓
      - Minerals: Calcium, Iron, Zinc, Magnesium, Potassium, Sodium, Phosphorus ✓
    Endpoints:
      - POST /v1/admin/import-usda (triggers USDA import) ✓
      - GET /v1/foods?category=X&source=usda (filtered search) ✓
    Tests:
      - Nutrient ingestion ✓ (8 unit tests passing)
      - JSON streaming decoder ✓
      - Nutrient extraction correctness ✓
      - Serving size from portions ✓
      - Atwater calorie fallback ✓


================================================================================
PHASE 5: NUTRITION TARGETS & MEAL REUSE
================================================================================

[x] 11. Nutrition Target Service
    Schema (optional):
      - user_nutrition_profiles table (id, user_id, calories_target, protein_target, carbs_target, fat_target, fiber_target, is_override, created_at, updated_at)
    Target Outputs:
      - Calories, protein, carbs, fat, optional fiber
    Inputs:
      - Goal, weight, height, date_of_birth/age, activity_level, workout_frequency
    Endpoints:
      - GET /v1/users/{user_id}/nutrition-targets
    Business Rules:
      - Show both calculated target and any manual override
      - Keep formulas explicit and deterministic
    Tests:
      - Each supported goal (build_muscle, lose_fat, maintain)
      - Each activity level
      - Override precedence
    Files:
      - services/nutrition_targets.go

[x] 12. Meal Reuse Features
    Schema:
      - favorite_foods table (id, user_id, food_id, created_at)
      - optional: favorite_meals table
    Endpoints:
      - GET /v1/meals/recent?days=7
      - POST /v1/meals/{id}/clone
      - GET /v1/foods/recent
      - POST /v1/foods/{id}/favorite
      - DELETE /v1/foods/{id}/favorite
      - GET /v1/users/{user_id}/favorites
    Business Rules:
      - Recent meals are user-scoped and date-bounded
      - Meal cloning duplicates meal foods, not just the meal row
      - Recent foods derived from user's meal history
    Tests:
      - 7-day and 30-day windows
      - Clone integrity
      - No cross-user leakage

[x] 13. Recipes as Reusable Nutrition Objects
    Schema:
      - recipes table (id, user_id, name, servings, notes, created_at, updated_at)
      - recipe_items table (id, recipe_id, food_id, quantity, created_at)
    Endpoints:
      - POST /v1/recipes
      - GET /v1/recipes
      - GET /v1/recipes/{id}
      - PATCH /v1/recipes/{id}
      - DELETE /v1/recipes/{id}
      - GET /v1/recipes/{id}/nutrition
      - POST /v1/recipes/{id}/log-to-meal
    Business Rules:
      - Recipes are reusable building blocks
      - Logging a recipe to a meal creates actual MealFood rows
      - Nutrition computed from ingredient totals and serving count
    Tests:
      - Nutrition calculation
      - Serving scaling
      - Recipe-to-meal expansion

================================================================================
PHASE 6: SUMMARIES & INTEGRATION RULES
================================================================================

[x] 14. Richer Daily & Weekly Nutrition Summaries
    Endpoints:
      - Extend current daily summary
      - GET /v1/users/{user_id}/weekly-summary
    Response Additions:
      - Consumed calories/macros
      - Target calories/macros
      - Deltas versus target
      - Meal counts
      - Micronutrient totals
      - Flagged deficiencies
    Business Rules:
      - Deficiency flags start deterministic (low protein, low iron)
      - Weekly summary aggregates both intake and workout context
    Tests:
      - Delta correctness
      - Weekly aggregation windows
      - Deficiency threshold behavior

[x] 15. Workout-Nutrition Integration Engine
    Rules from Scope:
      - Leg day logged -> +200 kcal (mostly carbs)
      - No workout today -> -200 kcal, protein target maintained
      - High volume week (20+ sets/muscle) -> +15% calories
      - 30 min cardio logged -> +300 kcal bonus
      - Recovery warning when training load high AND protein too low
      - Goal-alignment warning when intake conflicts with stated goal
    Implementation:
      - Central integration/rules service
      - Responses expose: final adjusted targets + explanation of which rules fired
    Endpoints:
      - Extend summary endpoints
      - GET /v1/users/{user_id}/recommendations
    Tests:
      - One test per rule
      - Combined rule precedence
      - No silent adjustments without explanation payload
    Files:
      - services/integration_rules.go

================================================================================
PHASE 7: PLATFORM FEATURES
================================================================================

[x] 16. Exports & GDPR Workflows
    Schema:
      - exports table (id, user_id, format, status, file_path, created_at, completed_at)
      - optional: deletion_requests table
    Endpoints:
      - POST /v1/exports (create export job)
      - GET /v1/exports/{id} (check status, download when ready)
      - POST /v1/account/delete-request
    Export Formats:
      - JSON first
      - CSV second
    Export Scope:
      - User profile
      - Workouts, workout exercises, workout sets
      - Cardio entries
      - Meals, meal foods
      - Foods, recipes
      - Weight entries
      - Settings and targets
    Tests:
      - Export job creation
      - Export package completeness
      - Ownership and auth checks

[ ] 17. Notifications
    Schema:
      - notifications table (id, user_id, type, title, message, payload_json, read_at, created_at)
    Endpoints:
      - GET /v1/notifications
      - PATCH /v1/notifications/{id}/read
    Trigger Sources:
      - Low protein warning
      - Missed meal logging
      - Workout reminder
      - Rest-day warning
      - Export ready
    Realtime Delivery:
      - WebSocket or SSE can come after persistence
    Tests:
      - Notification creation from rule outputs
      - Mark-read flow
      - No duplicate spam for same trigger window

[ ] 18. Two-Factor Authentication (2FA)
    Schema:
      - two_factor_secrets table (id, user_id, secret, verified, created_at)
      - recovery_codes table (id, user_id, code_hash, used_at, created_at)
    Endpoints:
      - POST /v1/auth/2fa/setup
      - POST /v1/auth/2fa/verify
      - POST /v1/auth/2fa/disable
      - POST /v1/auth/2fa/recover (use recovery code)
    Business Rules:
      - TOTP-based flow (Google Authenticator compatible)
      - Recovery code fallback (generate 8-10 codes on setup)
      - Login challenge after password verification
    Tests:
      - Valid TOTP
      - Invalid TOTP
      - Recovery code consumption
      - Disable flow

================================================================================
STRICT DEFERALLS (Do NOT schedule until deterministic backend is stable)
================================================================================

- AI workout program generation
- Adaptive TDEE AI
- RAG nutrition Q&A
- Meal-planning AI
- Grocery-list AI
- Progress prediction AI
- OAuth login (Google/Apple)
- WebSocket real-time updates (SSE preferred first)

================================================================================
RECOMMENDED BUILD ORDER
================================================================================

1.  Service layer                                    [Phase 0]
2.  Refresh tokens and sessions                      [Phase 1]
3.  Workout analytics services                       [Phase 2]
4.  Workout read-model endpoints                     [Phase 2]
5.  Streaks and adherence                            [Phase 2]
6.  Cardio entries                                   [Phase 3]
7.  Workout templates                                [Phase 3]
8.  Workout programs                                 [Phase 3]
9.  Food ingestion and catalog expansion             [Phase 4]
10. Normalized micronutrients                        [Phase 4]
11. Nutrition-target service                         [Phase 5] ✓
12. Meal reuse                                       [Phase 5] ✓
13. Recipes                                          [Phase 5] ✓
14. Richer summaries                                 [Phase 6] ✓
15. Workout-nutrition rule engine                    [Phase 6] ✓
16. Exports and GDPR workflows                       [Phase 7] ✓
17. Notifications                                    [Phase 7]
18. 2FA                                              [Phase 7]

================================================================================
FILES TO UPDATE FOR EACH FEATURE
================================================================================

Every feature should update:
- database/migrations.go    (schema changes)
- api/server.go             (route registration)
- api/openapi.yaml         (API documentation)
- tests                    (unit + integration tests)
