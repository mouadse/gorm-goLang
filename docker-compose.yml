services:
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fitness-migrate
    command: ["migrate"]
    environment:
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/fitness_tracker?sslmode=disable
      - APP_ENV=${APP_ENV:-development}
      - GORM_LOG_LEVEL=${GORM_LOG_LEVEL:-warn}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    networks:
      - fitness-network

  # Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fitness-app
    command: ["api"]
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/fitness_tracker?sslmode=disable
      - JWT_SECRET=${JWT_SECRET:?set JWT_SECRET}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-google/gemini-2.0-flash-001}
      - APP_ENV=${APP_ENV:-development}
      - GORM_LOG_LEVEL=${GORM_LOG_LEVEL:-warn}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-}
      - CORS_ALLOWED_METHODS=${CORS_ALLOWED_METHODS:-}
      - CORS_ALLOWED_HEADERS=${CORS_ALLOWED_HEADERS:-}
      - CORS_ALLOW_CREDENTIALS=${CORS_ALLOW_CREDENTIALS:-}
      - CORS_MAX_AGE_SECONDS=${CORS_MAX_AGE_SECONDS:-}
      - EXERCISE_LIB_URL=http://exercise-lib:8000
      - DB_MAX_OPEN_CONNS=${DB_MAX_OPEN_CONNS:-25}
      - DB_MAX_IDLE_CONNS=${DB_MAX_IDLE_CONNS:-5}
      - DB_CONN_MAX_LIFETIME=${DB_CONN_MAX_LIFETIME:-30m}
      - DB_CONN_MAX_IDLE_TIME=${DB_CONN_MAX_IDLE_TIME:-5m}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8080/readyz"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - fitness-network

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fitness-worker
    command: ["worker"]
    environment:
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/fitness_tracker?sslmode=disable
      - APP_ENV=${APP_ENV:-development}
      - GORM_LOG_LEVEL=${GORM_LOG_LEVEL:-warn}
      - DB_MAX_OPEN_CONNS=${DB_MAX_OPEN_CONNS:-10}
      - DB_MAX_IDLE_CONNS=${DB_MAX_IDLE_CONNS:-5}
      - DB_CONN_MAX_LIFETIME=${DB_CONN_MAX_LIFETIME:-30m}
      - DB_CONN_MAX_IDLE_TIME=${DB_CONN_MAX_IDLE_TIME:-5m}
      - WORKER_EXPORT_POLL_INTERVAL=${WORKER_EXPORT_POLL_INTERVAL:-15s}
      - WORKER_ADMIN_REFRESH_INTERVAL=${WORKER_ADMIN_REFRESH_INTERVAL:-1h}
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    networks:
      - fitness-network

  # Exercise Library Service
  exercise-lib:
    build:
      context: ./exercises_lib_service
      dockerfile: Dockerfile
    container_name: fitness-exercise-lib
    env_file:
      - ./exercises_lib_service/.env
    ports:
      - "8000:8000"
    cpus: "${APP_CPUS:-2.0}"
    environment:
      - PORT=8000
      - EXERCISE_DATA_DIR=/app/exercise_data
      - CATALOG_CACHE_DIR=/app/.catalog_cache
      - EMBEDDING_CACHE_DIR=/var/cache/fastembed
      - EMBEDDING_THREADS=${EMBEDDING_THREADS:-8}
      - EMBEDDING_PARALLEL=${EMBEDDING_PARALLEL:-4}
      - EMBEDDING_BATCH_SIZE=${EMBEDDING_BATCH_SIZE:-64}
    volumes:
      - ./exercises_lib_service/exercise_data:/app/exercise_data:ro
      - ./exercises_lib_service/.catalog_cache:/app/.catalog_cache
      - fastembed_cache:/var/cache/fastembed
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - fitness-network

  # Database
  postgres:
    image: postgres:16-alpine
    container_name: fitness-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fitness_tracker
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - fitness-network

  # Database Admin UI
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: fitness-pgadmin
    ports:
      - "8081:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@fitness-tracker.com
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-change-me}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - fitness-network

  # Prometheus - Metrics Collection
  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: fitness-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      app:
        condition: service_healthy
    networks:
      - fitness-network

  # Grafana - Visualization & Dashboards
  grafana:
    image: grafana/grafana:11.3.0
    container_name: fitness-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3000
    volumes:
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - grafana_data:/var/lib/grafana
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      prometheus:
        condition: service_healthy
    networks:
      - fitness-network

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:
  fastembed_cache:

networks:
  fitness-network:
    driver: bridge
